pytest>=8.3.4
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
# Pins homeassistant==2026.8.0 and requires Python >=3.14. Keep this floor at or
# above the HA version declared in custom_components/evon/manifest.json — a lower
# floor lets pip silently backtrack to an ancient harness (see: 0.13.205 resolved
# HA 2025.1.4 under Python 3.12, so tests never exercised the supported HA).
pytest-homeassistant-custom-component>=0.13.354
ruff>=0.14.0
mypy>=1.19.0
# Required by homeassistant.components.camera.img_util (imported at module level).
# Without this, any test that imports our camera.py fails on collection.
# DO NOT REMOVE — see commit 450cf26 and 7d82b82 for history.
PyTurboJPEG>=1.7.0
